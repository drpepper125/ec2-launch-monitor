# EC2 Launch Monitor Configuration Template
# Copy this file to config.env and customize the values for your environment

# AWS Configuration
AWS_REGION=us-east-2
LAMBDA_CODE_BUCKET=your-unique-lambda-code-bucket-name

# Security Configuration
# Replace with your IP address in CIDR notation (e.g., 203.0.113.0/32)
# WARNING: 0.0.0.0/0 allows access from anywhere - replace with your actual IP!
ALLOWED_IP_ADDRESS=0.0.0.0/0

# CloudFormation Stack Name
STACK_NAME=ec2-launch-monitor
